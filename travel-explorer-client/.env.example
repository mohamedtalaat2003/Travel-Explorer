# Leave empty in dev to use the Vite proxy (requests to /api are forwarded to the backend).
# In production set this to the API origin, e.g. https://api.yourhost.com
VITE_API_BASE_URL=

# Backend HTTPS dev URL that the Vite dev proxy forwards /api to.
VITE_DEV_API_TARGET=https://localhost:7133
